# Image generation provider
# NODE_ENV=development -> API returns 1 preview; production -> 3 previews

OPENAI_API_KEY=
# Optional: override style advisor model/temperature
OPENAI_STYLE_ADVISOR_MODEL=gpt-4o-mini
STYLE_ADVISOR_TEMPERATURE=0.4
# Optional: override default image model
OPENAI_IMAGE_MODEL=gpt-image-1

# Required - OpenAI API key for text generation and image creation
OPENAI_API_KEY=your_openai_api_key_here

# Optional - Temporal configuration (defaults shown)
TEMPORAL_ADDRESS=localhost:7233
TEMPORAL_NAMESPACE=default

# Optional - Data directory (defaults to ./data/books)
BOOKS_DATA_DIR=./data/books

# Optional - Enable cover generation (default: false)
ENABLE_COVER_GENERATION=false

# Note: All other settings (models, timeouts, feature flags) are configured
# in temporal/src/shared.ts for simplicity and type safety
